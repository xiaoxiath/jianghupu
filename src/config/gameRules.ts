/**
 * @file 游戏核心规则与配置
 * @description 用于集中管理游戏中的“魔法数字”和平衡性参数。
 */

export const FACTION_RULES = {
  // 当两个门派的敌对度达到此值时，会触发宣战事件。
  WAR_DECLARATION_THRESHOLD: 100,
};

export const BATTLE_RULES = {
  // 可以在此定义伤害计算公式、闪避率等
};

export const CULTIVATION_RULES = {
  // 可以在此定义境界提升所需经验值等
};